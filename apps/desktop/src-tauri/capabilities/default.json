{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop permissions for QuantGlass.",
  "windows": ["main"],
  "permissions": ["core:default", "notification:default"]
}
